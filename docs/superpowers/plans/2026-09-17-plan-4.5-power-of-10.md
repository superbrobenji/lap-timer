# Plan 4.5: Power of 10 Compliance Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax. Sessions 4.5.2–4.5.6 are appended to this document at the start of each session (like plans 02–04), once the tooling and prior retrofits they build on exist.

**Goal:** Bring the shipped on-device firmware C into strict compliance with the ten rules of Power of 10 (Holzmann/JPL) and keep it compliant, via CI static analysis and an adversarially-justified deviation register — behavior-preserving throughout.

**Architecture:** A custom text linter (`tools/lint/power_of_10.py`) enforces the project-specific gates (function length, assertion density, no heap after init, no unregistered function pointer, restricted preprocessor) directly on the source; `clang-tidy` (over the IDF and host compile databases) and `cppcheck` add the deeper control-flow/pointer/return-value checks (rules 1/6/7/9 and "multiple analysers"). All three run report-only in CI while the retrofit lands, then flip to blocking required checks. The retrofit changes only *how* code is written; the existing host tests, `core_selftest` on the ESP32, the PBM UI goldens, the replay fixtures, and the on-device measurements are the regression net.

**Tech Stack:** ESP-IDF v5.3.2 (built via `tools/idf-env.sh`), C11, the host test harness (Unity, clang `test/build` + gcc-16 `test/build-gcc`), `clang-tidy`/`cppcheck` (CI), Python 3 (the linter). No new firmware dependency.

**Spec:** `docs/superpowers/specs/2026-09-17-power-of-10-compliance-design.md` — the authority for the rule interpretations, the scope, the strict+adversarial-deviation model, and the delivery order (rule 9 last). Read it before any task.

## Global Constraints

- **Scope of the rules:** `components/core/**`, `components/app/**`, `components/drivers/**`, `main/**` (the shipped on-device C) obey all ten rules per the design §3. `tools/**` and `test/**` are exempt from the ten rules but MUST stay zero-warning (rule 10). Vendored / managed / ESP-IDF code is fully exempt: `test/unity/**`, the vendored `jsmn` parser (`components/core/util/jsmn.c` and `components/core/include/core/jsmn.h`), `managed_components/**`, and anything under `$IDF_PATH`.
- **Behavior-preserving:** no interface change, no behavior change, no feature change ships in this plan. Every session keeps the full existing test suite, the PBM goldens, the replay fixtures, and (for firmware sessions) the on-device behavior identical. A firmware session flashes the affected env to confirm (the standing build-and-flash rule; wait for the user's "ready").
- **Deviation burden of proof (design §2):** a function pointer or other exception is registered in `docs/power-of-10-deviations.md` only after a spelled-out compliant alternative is shown to genuinely fail or clearly not be worth its cost. Default is comply. The register row records the rejected alternative, not just a justification.
- **Refined forms (adopted 2026-09-19, mid-plan):** we follow Holzmann's *refined* rules (`spinroot.com/p10`), not only the 2006 paper — see design §3. This tightened rules 4, 5, 6, and 8 after sessions 4.5.1–4.5.3 had merged; the linter/spec carry the refined gates from 2026-09-19 on, findings they surface in already-merged core/app are recorded, and the **blocking-flip session (4.5.6) re-verifies the whole tree and sweeps them clean** (or registers them).
- **Rule 2:** every loop has a static upper bound; the review checklist also confirms a `for`-loop index is not modified inside the loop body (refined rule 2).
- **Rule 4:** ≤ 60 code lines per function AND ≤ 30 code lines per compound-statement body (`if`/`else`/`for`/`while`/`do`/`switch` block) — both hard gates, blank/comment-only/brace-only lines excluded. Over-limit bodies are extracted into named helpers or registered.
- **Rule 5 (refined per-function):** ≥ 2 meaningful assertions for **every function of > 20 code lines**; functions ≤ 20 code lines are **exempt**. No `assert(true)`/tautological padding — an assertion counts only if it can genuinely fail. Via `CORE_ASSERT_RET`/`CORE_ASSERT_VOID` (core) or `LT_ASSERT_RET`/`LT_ASSERT_VOID` (app/drivers/main), over the `core_set_assert_hook` reporter — log to the §17.7 error ring and return a safe value, NEVER abort on the device; side-effect-free. The per-component average is an informational metric only, not the gate.
- **Rule 6:** smallest scope AND correct linkage — a file-only object is file `static`, a function-only-persistent object is function-local `static`. Enforced by `-Wmissing-variable-declarations`/`-Wmissing-prototypes` (added at 4.5.6) + review.
- **Rule 8 (refined):** the `##`/`#x` gate stays, plus: no `#undef` in shipped code; no function-like/logic macros in `.c` files (object-like constant `#define`s in a single `.c` are permitted — rule 8 sanctions simple macros and moving them to a header would break rule 6); `#ifdef` budget (non-guard conditionals ≤ header count) tracked as an advisory count.
- **Rule 9 is done LAST** (session 4.5.5); the linter's no-unregistered-function-pointer gate stays inert until then.
- **Existing build flags stay:** `-Wall -Wextra -Werror -Wshadow` + `-Wconversion` (non-fatal), ASan/UBSan on host — the analysers add to, never replace, these.
- **Subagent models:** never dispatch a subagent on Fable; haiku/sonnet/opus by task tier.
- **Every commit** ends with the two trailers:
  ```
  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_014KyGHgU5MeENuESPYuKjED
  ```
- Working directory: repository root `/Users/benji/projects/personal/lap-timer`.

## Parallel execution rules

Session 4.5.1 is scaffolding (the linter + register + macro + CI) and is serial: the CI job depends on the linter, and the retrofit sessions all depend on the analysers existing. Sessions 4.5.2–4.5.4 (component retrofits) are sequential in the plan (each is a full component's behavior-preserving refactor with its own review + flash), but WITHIN a session the per-file edits are batch-friendly and may be split across parallel worktree tasks where the files are disjoint — each session states its own split at its start. Session 4.5.5 (rule 9) touches `core` and its consumers together and is serial. Session 4.5.6 is serial (a CI/process change + the whole-plan review).

## File structure produced by this plan

```
tools/lint/power_of_10.py         custom linter: length, assertion density, no-heap,
                                  restricted preprocessor, no-unregistered-function-pointer
                                  (inert until 4.5.5); reads the deviation register
docs/power-of-10-deviations.md    the deviation register (design §4 format)
.clang-tidy                        clang-tidy check set mapping to rules 1/4/6/7/9
components/app/include/app/lt_assert.h   LT_ASSERT_RET/VOID over the core assert hook
.github/workflows/*                a report-only "static-analysis" job (4.5.1), flipped
                                  to a blocking required check (4.5.6)
```
The retrofit sessions (4.5.2–4.5.5) modify existing `components/**` and `main/**` sources in place; they add no new production files beyond the above.

---

## Session 4.5.1 — linter, deviation register, assert macro, CI report-only (design §2/§4/§5)

Roadmap exit: the three analysers run as report-only CI jobs; the deviation register and `LT_ASSERT_*` exist; zero behavior change; tag `p4.5-d1`. SERIAL (2 tasks). Ruling: this session adds NO source-code compliance edits to `components/**` (that is 4.5.2+) — it only builds the enforcement machinery and proves it runs green (report-only) against the current, not-yet-compliant tree. The linter is the primary project-specific gate (text-based, no compile database needed); `clang-tidy`/`cppcheck` are the deeper second/third analysers.

### Task 1: the custom linter + deviation register + `LT_ASSERT_*`

**Files:** create `tools/lint/power_of_10.py`, `docs/power-of-10-deviations.md`, `components/app/include/app/lt_assert.h`. No source edits under `components/**`/`main/**`.

**Interfaces produced:**
- `tools/lint/power_of_10.py [--paths ...] [--register docs/power-of-10-deviations.md] [--enforce-fnptr] [--json]` — scans the in-scope C (default: `components/core components/app components/drivers main`, excluding the vendored/exempt paths in the design §1). Checks, each reporting `file:line: RULE-N: <msg>`:
  - **rule 4** function length > 60 code lines (exclude blank/comment-only/brace-only lines; count from the `{` of a top-level function definition to its matching `}`);
  - **rule 5** per-component (per top-level dir) average assertions/function < 2 (count `CORE_ASSERT_*`/`LT_ASSERT_*` calls ÷ function count; report the average, not per-function);
  - **rule 3** any `\b(malloc|calloc|realloc|free|strdup|aligned_alloc)\s*\(` in in-scope code (heap);
  - **rule 8** `##` token-paste or `#<ident>` stringisation-as-logic in in-scope code (allow it inside the exempt X-macro/test paths);
  - **rule 9** (only when `--enforce-fnptr`) a function-pointer typedef/parameter/variable whose `file:symbol` is not in the register — INERT by default this session.
  It reads the register (skips registered sites), prints a summary, and exits non-zero only when a NON-report-only invocation finds an unregistered violation. Default mode is report-only (always exit 0, print findings). Pure-Python, no third-party deps.
- `docs/power-of-10-deviations.md` — the design §4 table (`id | rule | file:symbol | compliant alternative considered | why rejected | benefit | reviewer`), seeded with ONLY the genuinely-unavoidable IDF/FreeRTOS boundary function pointers (task entry `xTaskCreateStatic*`, `gpio_isr_handler_add`/`board_buttons_enable_isr` handler, `esp_timer` callbacks) — each row's "why rejected" = "the IDF/FreeRTOS API signature is a function pointer; no compliant alternative without abandoning the SDK". The codebase's own callbacks are NOT seeded (they are reworked in 4.5.5, not registered).
- `components/app/include/app/lt_assert.h` — `LT_ASSERT_RET(cond, code, ret)` / `LT_ASSERT_VOID(cond, code)`, mirroring `core/core.h`'s macros but callable from the app/driver layer (which may not include `core/core.h` directly): they call the same `core_assert_fail(code, __FILE__, __LINE__)` reporter (so app assertions land in the §17.7 error ring via the installed hook) and `return (ret)` / `return`. No behavior change (nothing calls them yet).

- [ ] **Step 1** — write `power_of_10.py`: the file walker (in-scope paths minus exempt), a light C tokenizer sufficient for the checks (strip strings/char-literals/comments; find top-level `{...}` function bodies by brace matching at column-0 definitions), and the five checks above. Report-only by default.
- [ ] **Step 2** — run it on the current tree: `python3 tools/lint/power_of_10.py`. Capture the CURRENT findings (expected: ~11 rule-4 over-length functions, rule-5 averages well under 2, zero rule-3 heap, rule-8 clean). This baseline is the retrofit's worklist — record the counts in the ledger.
- [ ] **Step 3** — write `docs/power-of-10-deviations.md` with the header + the IDF/FreeRTOS boundary seed rows only.
- [ ] **Step 4** — write `components/app/include/app/lt_assert.h`; confirm it compiles by including it in one app TU under a throwaway build (do not leave the include behind). No `components/**` source is changed for compliance yet.
- [ ] **Step 5** — commit (`feat(lint): power_of_10.py linter + deviation register + LT_ASSERT macro (report-only)`).

### Task 2: CI report-only static-analysis job (`clang-tidy` + `cppcheck` + the linter)

**Files:** modify the CI workflow(s) under `.github/workflows/` (add a `static-analysis` job); create `.clang-tidy`. No source edits.

**Rulings:** `clang-tidy` needs a compile database — use the ones the existing CI already produces: the host CMake (`-DCMAKE_EXPORT_COMPILE_COMMANDS=ON` on `test/build`) covers `components/core`; the `firmware.yml` `idf.py build` of `moto_neo6m` emits `build/moto_neo6m/compile_commands.json` covering `components/app`/`drivers`/`main`. Run `clang-tidy` against each database over the in-scope files only. `cppcheck` runs standalone over the in-scope tree with the project include dirs (`--enable=warning,style,portability --std=c11 --error-exitcode=0` while report-only). All three are **report-only** this session (`|| true` / `continue-on-error`), NOT required checks — they print findings to the job log without failing the build.

- [ ] **Step 1** — `.clang-tidy`: enable the rule-aligned checks (`misc-no-recursion` r1, `readability-function-size` with `LineThreshold: 60` r4, `bugprone-*`, `cert-err33-c`/`bugprone-unused-return-value` r7, pointer/`readability-*` for r6/r9) and disable the noisy/irrelevant ones; document each mapping in a comment.
- [ ] **Step 2** — add the `static-analysis` CI job: build the host tree with `CMAKE_EXPORT_COMPILE_COMMANDS=ON` and (reusing the firmware build) run `clang-tidy -p <db>` over the in-scope files, `cppcheck` over the in-scope tree, and `python3 tools/lint/power_of_10.py`. All `continue-on-error: true` (report-only). Upload/echo the findings.
- [ ] **Step 3** — push and confirm the job RUNS and REPORTS on the PR without failing it (report-only), and that the existing five required checks are unaffected. The `static-analysis` job is NOT added to branch-protection required checks yet (that is 4.5.6).
- [ ] **Step 4** — commit (`ci: report-only Power-of-10 static-analysis job (clang-tidy + cppcheck + linter)`).

---


## Session 4.5.2 — retrofit `components/core` to rules 1–8, 10 (behavior-preserving)

Roadmap exit: the core linter reports clean (avg assertions ≥ 2, 0 functions > 60 code lines, 0 heap/preprocessor); host tests + `core_selftest` green on the ESP32; tag `p4.5-d2`. PARALLEL: a worktree wave of 4 disjoint sub-component tasks, then serial integration. Function pointers are LEFT untouched (rule 9 is 4.5.5).

**Shared retrofit contract (every task, every file it touches):**
- **Behavior-preserving. No interface change, no logic change.** Only: split over-length functions into named static helpers; add assertions; bound loops; make return-value handling explicit; tighten scope. The existing host tests and the replay fixtures must stay byte-for-byte green — that is the proof of no regression.
- **Rule 4:** any function > 60 code lines (blank/comment/brace-only excluded) is split into named `static` helpers with clear responsibilities. The 4 core offenders: `fus_step` (fusion), `lap_on_fix` (lapengine, 109 lines), `drag_on_fused` (dragengine), `exp_json_feed` (export).
- **Rule 5:** raise assertion density so the FILES THE TASK TOUCHES average ≥ 2 assertions/function (so the whole `components/core` averages ≥ 2). Use `CORE_ASSERT_RET(cond, code, ret)` / `CORE_ASSERT_VOID(cond, code)` (`core/core.h`). Assert REAL anomalies only — pointer params non-NULL, array indices/counts in range, enum/state values valid, numeric ranges the function assumes, key postconditions/invariants. NEVER tautologies (`CORE_ASSERT(x==x)`), never a condition with a side effect, never a check the types already guarantee. Trivial one-line accessors may carry zero — the density comes from the substantive functions. Pick a plausible `code` from `core/err.h`/§17.7 (or the nearest existing code); do not invent a behavior. A failing assertion reports + returns a safe value (it must NOT change the success-path behavior the tests observe).
- **Rule 2:** every loop has a statically evident bound. Where a `while`/scan loop is not already bounded by a fixed capacity, add an explicit iteration cap (`for (int i = 0; i < CAP && ...; i++)`) sized to the real maximum, with a `CORE_ASSERT` if the cap is hit.
- **Rule 7:** every non-`void` call result is used or explicitly `(void)`-cast with intent; parameter validation is the rule-5 assertions.
- **Rules 6/1/8:** narrow any over-broad scope; no recursion/goto (none expected); no new complex macros.
- **Generated/data files are exempt:** `components/core/ui/fonts.c` is generated glyph data (source is `tools/fonts/gen_fonts.py`) and `icons.c` is hand-authored bitmap data — do not add assertions to the data tables; add the fonts.c path to the linter's exempt list (integration step). `jsmn` is already exempt.
- **Verify per task:** build the host test tree (`cmake --build test/build && ctest --test-dir test/build`) green for the areas the task's files feed; run `python3 tools/lint/power_of_10.py --paths <the task's dirs>` and confirm its files hit avg ≥ 2 and 0 over-length.

**Parallel wave (worktrees, disjoint sub-dirs):**
- **Task 1 (opus) — `fusion/` + `geo/`:** split `fus_step`; assertions across `fus.c`/`fus_still.c`/`fus_orient.c`/`fus_fwd.c`/`geo.c` (the §9.3 filter states, orientation, ENU projections — rich invariants).
- **Task 2 (opus) — `lapengine/` + `dragengine/` + `timebase/`:** split `lap_on_fix` (109 lines) and `drag_on_fused`; assertions across `lap.c`/`lap_gate.c`/`drag.c`/`tb.c` (state machines, sector/gate indices, §6.6 dead-reckoning ranges, mono/gps time monotonicity).
- **Task 3 (sonnet) — `export/` + `session/` + `config/`:** split `exp_json_feed`; assertions across `exp*.c`/`ses*.c`/`cfg*.c` (record type/length bounds, buffer capacities, config field ranges).
- **Task 4 (sonnet) — `tracks/` + `util/` + `ui/`:** assertions across `trk*.c`/`json.c` (not `jsmn`) /`render.c`/`screens_moto.c` (venue/layout counts, gate indices, framebuffer bounds); `fonts.c`/`icons.c` exempt (data).

**Serial integration (after the wave):**
- [ ] Merge the four worktree branches (disjoint files → clean); add `components/core/ui/fonts.c` to the linter exempt list.
- [ ] Build host `test/build` (clang) and `test/build-gcc` (gcc-16); `ctest` all green (byte-exact goldens/fixtures unchanged = behavior preserved).
- [ ] `python3 tools/lint/power_of_10.py --paths components/core`: `components/core` avg ≥ 2.0, rule4 = 0, rule3 = 0, rule8 = 0.
- [ ] Build `moto_sim` + `moto_neo6m`; flash `moto_sim` (await "ready") and re-run the p03-d4 sim-lap check — laps still 28071/28044/28028 ms (core behavior identical on target); `core_selftest` still passes on the ESP32.
- [ ] Whole-session review (spec compliance + assertion quality: no tautologies, real anomalies, behavior-preserving); one fix pass; commit + tag `p4.5-d2`.

---


## Session 4.5.3 — retrofit `components/app` to rules 1–8, 10 (behavior-preserving)

Roadmap exit: the app linter reports clean (avg ≥ 2, 0 over-length, 0 heap/preprocessor); both firmware envs build; on-device behavior unchanged (flash); tag `p4.5-d3`. PARALLEL: 3 worktree tasks over disjoint app sub-components, then serial integration. Function pointers LEFT untouched (rule 9 is 4.5.5).

**Shared contract (same as 4.5.2's, adapted for app):**
- **BEHAVIOR-PRESERVING. No interface/logic/observable-behavior change.** Only: split over-length functions into named static helpers; add assertions; bound loops; explicit return handling.
- **No host-test net for app** (app is IDF-only; core_selftest tests core, not app). So the regression gate is: both firmware envs build green under `-Wall -Wextra -Werror -Wshadow`, AND the integration flash re-runs the plan-03/04 on-device behaviors and confirms them IDENTICAL. Each task must diff its over-length-function split with assertion lines stripped and confirm pure code-motion.
- **Rule 5 — LT_ASSERT, real anomalies only:** use `LT_ASSERT_RET(cond, code, ret)` / `LT_ASSERT_VOID(cond, code)` (`app/lt_assert.h`, over the same core hook → §17.7 error ring, never abort on target). Assert genuine anomalies: non-NULL params, indices/counts in range, valid state/enum, invariants. Per-module codes in the 0x0B** range (avoids core's 0x0A** and the real §17.7 0x01-09**): pipeline 0x0B10, logger 0x0B20, supervisor 0x0B30, cmd 0x0B40, ui 0x0B50, sys/nvs 0x0B60, rtc 0x0B70, ipc 0x0B80. Per-COMPONENT (components/app) average ≥ 2; trivial accessors may carry zero, density comes from substantive functions — NEVER pad with redundant non-NULL on internal helpers whose caller already validated (the 4.5.2 lesson).
- **CRITICAL — do NOT assert on expected/untrusted input (the ses decode lesson):** the `cmd` dispatch rejecting an unknown op or malformed request payload, the console parsing a bad command, a queue-receive returning empty, a bad OTA/BLE frame — these are EXPECTED runtime conditions the code is designed to handle, NOT anomalies. Leave them as plain `if ... return`/error paths. Assertions are for programmer-error / impossible states only.
- **Rule 4:** split `on_fix` (pipeline.c, 63 lines) and `handle_event` (ui.c, 73 lines) into named static helpers, behavior-identical. Split any other >60.
- **Rule 2:** explicit static bound on any unbounded loop (queue drains are bounded by depth; the console/cmd stream loops may need caps). **Rules 6/1/8:** tighten scope, no recursion/goto, no new complex macros. **Rule 3:** confirm no malloc after init (app already complies).
- Verify per task: build BOTH envs (`source tools/idf-env.sh`; `./build.sh moto_neo6m build`, `./build.sh moto_sim build`) green + within the 0x130000 gate; `python3 tools/lint/power_of_10.py --paths <task dirs>` → avg ≥ 2, 0 over-length. `dependencies.lock` stays 5.3.2.

**Parallel wave (worktrees, disjoint sub-dirs):**
- **Task 1 (opus) — `pipeline/` + `ui/`:** split `on_fix` and `handle_event`; assertions across pipeline.c (§9.1 fix/raw order, per-lap stats, RTC save/resume, the fusion/lap/drag calls) and ui.c/ui_buttons.c (model update, menu nav, button debounce, framebuffer). Note ui's fault-strip/menu already has some bounds.
- **Task 2 (sonnet) — `logger/` + `cmd/`:** assertions across logger.c (ring drains, batch buffer bounds, .sum/.log record building, safe-mode suppression) and cmd.c (op dispatch — but leave unknown-op/bad-payload as plain error returns per the CRITICAL note; assert only internal invariants like a non-NULL emit callback param, a chunk buffer capacity).
- **Task 3 (sonnet) — `supervisor/` + `sys/`:** assertions across sup.c/sup_errlog.c (hb bounds, task-registration indices, crash-loop/safe-mode logic) and sys/ lt_nvs.c/lt_rtc.c/lt_ipc.c/lt_sys.c (NVS blob sizes, RTC field validity, queue/ring init params, sys_flags bit range) — but the NVS/RTC blob LOAD validation of on-flash bytes is untrusted-input handling → keep as plain returns, not assertions.

**Serial integration:**
- [ ] Merge the 3 worktree branches (disjoint files → clean).
- [ ] `python3 tools/lint/power_of_10.py --paths components/app` → avg ≥ 2, rule4 = 0, rule3 = 0, rule8 = 0.
- [ ] Build `moto_sim` + `moto_neo6m` green, within gate.
- [ ] Review assertion quality (real anomalies, NO expected-input asserts, NO padding, splits byte-identical). One fix pass.
- [ ] Flash `moto_sim` (await "ready"): sim laps still 28071/28044/28028 ms; `dbg status`/`dbg mem` sane; `dbg laps` correct; the `ui` task still logs refresh dirty-boxes; a `dbg crash`×3 still trips safe mode. Behavior identical to plan-03/04.
- [ ] Commit + tag `p4.5-d3`.

---

---

## Session 4.5.4 — retrofit `components/drivers` + `main` to rules 1–8, 10 (behavior-preserving)

Roadmap exit: drivers + main report clean under the refined gates (0 rule-4/4-compound/5/8; every >20-line function >=2 genuine assertions); both firmware envs build 0-warning under `-Werror`; on-device flash confirms identical behavior; tag `p4.5-d4`. PARALLEL: 3 disjoint worktree tasks + serial integration. Function pointers LEFT untouched (rule 9 = 4.5.5).

**Shared contract (every task):**
- **BEHAVIOR-PRESERVING. No interface/logic/observable-behavior change.** Only: add assertions; split the one over-length function (app_main); bound the two unbounded loops; make return handling explicit; tighten scope. No host-test net for drivers/main → the regression gate is: both envs build under `-Wall -Wextra -Werror -Wshadow`, AND the integration flash re-runs the on-device behaviors identical.
- **Assertions (rule 5, refined):** drivers use `CORE_ASSERT_RET(cond, code, ret)` / `CORE_ASSERT_VOID(cond, code)` from `core/core.h` (all drivers depend on `core`; `board_devkit_v1` and `storage_internal` must ADD `core` to their `CMakeLists.txt` REQUIRES — they currently lack it). `main` already includes `core/core.h`. Give every **>20-code-line** function >=2 GENUINE assertions (the refined gate); functions <=20 lines are exempt — do NOT pad them, but a genuine non-NULL/param-range check on a public entry point is welcome where real. Assert REAL anomalies only (non-NULL params, indices/counts in range, valid enum/state, ranges the function assumes, key post/invariants) — never tautologies, never a side-effecting condition, never a check the types already guarantee. Per-file assert codes in the 0x0C** range (drivers) / 0x0C80 (main), each file defining its own `<MODULE>_ASSERT_CODE` like the app/core files do.
- **Rule 2:** add an explicit iteration cap to the two loops without a structural bound — `storage_internal.c` `sto_list` readdir loop and `export_serial.c` `ring_push` full-ring retry loop; other loops are already capacity/count-bounded (leave them, add a brief `/* rule 2: ... */` note where non-obvious).
- **Rule 7:** every non-`void` call result used or explicitly `(void)`-cast with intent.
- **Rules 1/6/8:** already clean (no goto/recursion; 0 rule-8; 0 rule-4-compound) — do not regress. Rule-6 static-linkage is enforced by `-Wmissing-*` at 4.5.6; opportunistically mark obvious file-local globals `static` if trivially safe, else leave for the sweep.

**Rule-5 worklist (the >20-line functions with 0 assertions today):**
board.c: `board_init` (60L), `board_battery_read_mv` (21L). storage_internal.c: `sto_mount` (27L), `sto_list` (21L). export_serial.c: `sframe_emit` (26L), `run_stream` (22L), `dbg_logtest` (39L), `cmd_dbg` (26L), `export_serial_start` (21L). gps_sim.c: `gps_poll` (26L). main/app_main.c: `app_main` (84L — also rule-4 >60, must be SPLIT into named static helpers first, then each helper/`app_main` asserted per the >20-line rule). The real-hardware drivers (`gps_neo6m.c`, `imu_mpu6050.c`, `imu_sim.c`) have no >20-line function → rule-5-exempt; add a genuine entry-point assert only where real, do not pad.

**Parallel wave (worktrees, disjoint):**
- **Task 1 (opus) — `main/app_main.c`:** split `app_main` (84 code lines) into named static helpers (boot phases — e.g. hardware/board init, storage mount, task spawn, resume/rtc) so `app_main` and each helper are <=60 lines; add >=2 genuine assertions to `app_main` and any resulting >20-line helper (boot preconditions: non-NULL handles, init return codes already checked → assert the invariant, reset-reason/enum range, safe-mode flag range). `MAIN_ASSERT_CODE 0x0C80`. Behavior-preserving: the boot sequence and its ordering are identical.
- **Task 2 (sonnet) — `board_devkit_v1/board.c` + `storage_internal/storage_internal.c` + `export_serial/export_serial.c`:** add `core` to board's and storage's `CMakeLists.txt` REQUIRES; assertions on the flagged functions; cap the two rule-2 loops (storage `sto_list` readdir, export_serial `ring_push` retry). Codes: `BOARD_ASSERT_CODE 0x0C10`, `STO_ASSERT_CODE 0x0C70`, `EXP_SERIAL_ASSERT_CODE 0x0C20`.
- **Task 3 (sonnet) — `gps_sim/gps_sim.c` + `gps_neo6m/gps_neo6m.c` + `imu_sim/imu_sim.c` + `imu_mpu6050/imu_mpu6050.c`:** assertions on `gps_poll` (the one flagged) + genuine entry-point asserts on the others where real (non-NULL out params, sample-count bounds, i2c/uart return invariants) — no padding. Codes: `GPS_SIM_ASSERT_CODE 0x0C40`, `GPS_NEO_ASSERT_CODE 0x0C30`, `IMU_SIM_ASSERT_CODE 0x0C60`, `IMU_MPU_ASSERT_CODE 0x0C50`.

**Serial integration:**
- [ ] Merge the three worktree branches; resolve the (disjoint) trees.
- [ ] `python3 tools/lint/power_of_10.py --paths components/drivers main` → 0 rule-4/4-compound/5/8; per-file diffs are code-motion (app_main split) + assertion lines only.
- [ ] Build both envs (`moto_sim`, `moto_neo6m`) 0-warning under `-Werror`.
- [ ] Review assertion quality (genuine anomalies, no padding, no expected-input asserts, app_main split byte-identical). One fix pass.
- [ ] Flash `moto_sim` (covers app_main + gps_sim + imu_sim + board + storage): confirm boot/laps/dbg/ui/safe-mode identical. The real-hw drivers (gps_neo6m/imu_mpu6050) are behavior-preserving + build-verified but not flash-tested absent hardware — noted.
- [ ] Commit, PR, CI green, merge, tag `p4.5-d4`.

---

## Session 4.5.5 — rule 9: function-pointer rework (LAST, serial, behavior-preserving)

Roadmap exit: every internal `(*name)(` callback reworked to a compliant alternative; `python3 tools/lint/power_of_10.py --enforce-fnptr` reports 0; only the IDF/FreeRTOS boundary remains (register PD-1..6, bare-identifier passing the linter does not match); host tests 31/31 + replay fixtures byte-exact + flash identical; tag `p4.5-d5`. SERIAL — these touch core public interfaces + shared consumers. Gate after EACH task. May span >1 sitting.

**Global contract:** behavior-preserving — the events/output/order are identical; only the delivery MECHANISM changes (callback -> caller-drained buffer / iterator / link-time function). Event wire codes (§12.3) never renumber. After each task: host tests (where they exist) + build both envs; flash at the end.

### Task 1 (assert hook -> weak link-time function) — core.c/core.h, sup, app_main, test support
Ruling (resolves the map's "hard" tension: link-time extern vs. tests' per-case dynamic swap): replace the stored `_Atomic core_assert_hook_t` + `core_set_assert_hook` + the `core_assert_hook_t` typedef with a plain function `void core_assert_report(uint16_t code, const char *file, int line)` that `core_assert_fail` calls directly. `core.c` provides a `__attribute__((weak))` **no-op default** (preserves "silent when nobody overrides" for the replay tool / any core-only link). The app provides a **strong** `core_assert_report` (move `app_assert_hook`'s body — ESP_LOGE + errlog_add — into it; drop `sup_install_assert_hook` and its call in app_main.c). Host tests provide a **strong** `core_assert_report` in a shared test-support TU that records into a resettable global (`lt_test_assert_reset()` / `lt_test_assert_count()` / last-code); `test_smoke.c` + `test_ses_frame.c` switch from install/uninstall to reset-then-inspect. Removes the function pointer entirely. Gate: host tests 31/31 (clang + gcc-16).

### Task 2 (sto_list -> caller array) — hal/storage.h, storage_internal.c, + 4 callers
`int sto_list(const char *dir, sto_entry_t *out, int cap)` returns entry count (or -1); `sto_entry_t { char name[...]; uint32_t size; }`. `cap==0`/`out==NULL` = count-only (preserves the current NULL-cb "just count" mode). Callers loop `for (i=0;i<n;i++)`: export_serial `fs_list_cb`->print loop; cmd.c `count_sum_cb`->count `.sum`; cmd.c `list_scan_cb` dedup-by-prefix logic moves verbatim into the caller loop over `out[]`; logger.c `evict_cb`->oldest-tracking loop. Gate: flash (no host tests).

### Task 3 (board s_btn_cb -> queue; delete dead PPS) — board.c, hal/board.h, ui_buttons.c
`board_buttons_enable_isr`'s param changes from `void(*cb)(uint8_t,int64_t)` to `QueueHandle_t q`; `btn_isr` does `xQueueSendFromISR(q, &(btn_raw_t){mask,now}, &hpw)` directly (keeps board.c ui-agnostic — it only knows "a queue of btn_raw_t"). `ui_buttons.c` passes `s_btn_q` instead of `btn_isr_cb`; delete `btn_isr_cb`. DELETE the dead PPS machinery (`s_pps_cb`, `board_pps_enable`, `pps_isr`, its `#if CFG_HAS_PPS` blocks — zero callers, confirmed dead); M10 PPS re-adds it in queue form in its future plan. `btn_raw_t` must be shared (move its def to hal/board.h or a shared header if ui_buttons owns it — check; keep the struct identical). Gate: flash (button presses drive UI menu nav).

### Task 4 (lap + drag events -> caller-drained buffer) — lap.h/c, drag.h/c, pipeline.c, replay_run.c, test_lap.c, test_drag.c
`lap_on_fix(lap_t*, const gps_fix_t*, const fused_sample_t*, event_t *out, int cap, int *n_out)` — the internal `emit` appends to `out[]` (CORE_ASSERT_VOID on `*n_out < cap` before writing), thread `(out,cap,n_out)` through the ~9 static helpers instead of `(cb,ctx)`. Same for `drag_on_fused(drag_t*, const fused_sample_t*, event_t *out, int cap, int *n_out)`. New header consts: `LAP_EVT_MAX (LAP_MAX_SECTORS+3)`, `DRAG_EVT_MAX (DRAG_MAX_GATES+3)`. Consumers drain: `event_t evs[LAP_EVT_MAX]; int n; lap_on_fix(...,evs,LAP_EVT_MAX,&n); for(i<n) <existing engine_cb body>(&evs[i]);` — pipeline.c `engine_cb`, replay_run.c `engine_cb`, and every test call site. Event content + ORDER identical. Gate: host tests 31/31 + replay `.expected.json` byte-exact (killarney_full, gps_dropout, drag_0_180, drag_0_320).

### Task 5 (ses reader -> pull iterator) — ses.h, ses_frame.c, export_serial.c, cmd.c, test_ses_frame.c, test_ses_records.c, tools/replay/lib/logio.c
Replace push-callback `ses_reader_feed(r,buf,n,cb,ctx)` / `ses_reader_flush(r,cb,ctx)` with a push-then-pull iterator preserving the payload-aliasing contract: `void ses_reader_push(ses_reader_t*, const uint8_t *buf, size_t n)` (stages input) and `int ses_reader_next(ses_reader_t*, uint8_t *type, const uint8_t **payload, uint8_t *len)` -> 1 frame decoded (payload aliases reader buf, valid until next push/next), 0 need-more, -1 error; `ses_reader_finish(ses_reader_t*)` replaces flush (drain via next()). Preserve the FEED_MAX_STEPS rule-2 bound reasoning inside next(). Consumers loop `ses_reader_push(&r, chunk, k); while (ses_reader_next(&r,&t,&p,&l)==1) { <cb body> }`. The byte-at-a-time test loop -> push(1)+drain. tools/replay/logio.c (host-tool, rules-exempt) adapts its wrapper to the iterator (its own `logr_cb_t` struct is out of scope, keep it). Highest churn (~20 sites). Gate: host tests 31/31 + replay fixtures.

### Task 6 (cmd emit -> caller-drained resumable) — cmd.h, cmd.c, export_serial.c  [NO host test net — flash-gated, careful]
Rework `cmd_dispatch(op,tag,payload,len, emit, ctx)` off the `cmd_emit_fn` pointer. Ruling: burden-of-proof says rework, not register. Model: a caller-owned `cmd_out_t { uint8_t *buf; size_t cap; size_t len; uint8_t tag; uint16_t seq; int status; }` sink that cmd.c APPENDS framed chunks to via ordinary `cmd_out` functions (not pointers); when the buffer would overflow the caller flushes and cmd continues. Preserve the return-value abort: `cmd_out`'s append sets `status` (0 ok / non-0 transport-abort) which the op handlers check and short-circuit exactly as they check `emit()`'s return today. OPEN/READ's existing two-pass (measure then print) maps naturally to the fill-then-drain protocol. export_serial `ser_emit`/`sframe_emit` become buffer-fill + flush drivers. If — after a genuine attempt — the resumable protocol proves disproportionate for the streaming READ path, register `cmd`'s single emit site with the burden-of-proof recorded (LAST resort, argue hard first). Gate: flash — exercise `dbg`/console STATUS + LIST + OPEN + READ on a real session file; confirm byte-identical replies.

### Task 7 (register + gate verify) — docs/power-of-10-deviations.md, linter
Remove nothing that's now gone (PPS deleted); confirm PD-1..6 still describe the surviving IDF/FreeRTOS boundary. Verify `python3 tools/lint/power_of_10.py --enforce-fnptr` reports **0** unregistered `(*name)(` findings tree-wide. (Bare-identifier-to-IDF detection stays out of scope — the boundary is a fixed, reviewed, registered set the regex linter does not match; enabling `--enforce-fnptr` as a blocking CI check happens at 4.5.6.) Gate: linter 0 + both envs build + final flash.

---

## Session 4.5.6 — blocking flip + core sweep + rule 6 + process (FINAL)

Roadmap exit: analysers flipped to blocking; the deferred core findings swept clean; rule 6 enforced; the review process wired; whole-plan verified; tag `p4.5-d6` = `plan-4.5-done`.

Done (all behavior-preserving, one serial code-sweep task + tooling/docs done in the integration session):
- **Core sweep** (the refined-gate findings deferred from the mid-plan adoption): cfg.c `CLAMP_U` → `clamp_u16`/`clamp_u8`; trk.c `PUT_FIELD` → `static inline put_field`; screens_moto.c `LAP2_ROW_Y` → `static inline lap2_row_y`; screens_moto.c's >30-line page switch → `render_lap_dispatch`/`render_drag_dispatch` (goldens byte-identical); cfg_json.c `apply` + lap.c `reset_to_no_venue` each gain one genuine assertion.
- **Rule 6:** `-Wmissing-prototypes -Wmissing-declarations` (`-Werror`) added to every in-scope component + `main` (the design's clang-only `-Wmissing-variable-declarations` is not gcc-valid; `-Wmissing-declarations` is the equivalent). Two findings fixed: `gps_sim_venue_json` gets a shared prototype in the generated `sim_capture.h` (via the gensim template); `app_main` gets a forward declaration.
- **Blocking flip:** the custom linter (`--enforce-fnptr --fail-on-violation`) and clang-tidy (`--warnings-as-errors`, over `components/core`) now fail the CI `static-analysis` job; cppcheck stays advisory. `.clang-tidy` trimmed to rule-mapped checks. The build jobs' `-Werror` set (incl. `-Wmissing-*`) gates rules 6/10.
- **Process:** product spec §17.9 points at the P10 design as authority (and reflects the link-time `core_assert_report`); design §5 records the final enforcement posture; `docs/power-of-10-checklist.md` is the reviewer checklist.

Verified: linter rules 3/4/4-compound/5/8/9 all zero; `--enforce-fnptr --fail-on-violation` exits 0; clang-tidy clean over core; host tests 31/31 (clang + gcc-16); PBM goldens + replay fixtures unchanged; both envs build 0-warning; `moto_sim` flash behavior identical (boot safe_mode=0, laps 28.071/28.044, dbg console, no assert misfires). **`plan-4.5-done`.**
